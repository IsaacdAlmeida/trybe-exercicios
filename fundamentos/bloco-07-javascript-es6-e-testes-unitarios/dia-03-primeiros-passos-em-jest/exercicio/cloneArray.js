/* const cloneArray = (array) => {
  return [...array]
}

module.exports = cloneArray */

/* const cloneArray = require('./cloneArray');

test('properlu clones array', () => {
  const array = [1, 2, 3]
  expect(cloneArray(array)).toEqual(array) // verifica se é igual (valores de memórias são diferentes, confere por conferência)
  expect(cloneArray(array)).not.toBe(array) // verifica se não é o mesmo objeto (mesmo espaço na memória)
}) */